{
  "name": "la-casa-del-vino-static",
  "version": "2026.1.0",
  "private": true,
  "description": "Modernización mantenida del sitio estático histórico La Casa del Vino",
  "scripts": {
    "build-css": "sass --style=expanded --no-source-map scss/style.scss css/style.css",
    "watch-css": "sass --watch --style=expanded --no-source-map scss/style.scss css/style.css",
    "validate-site": "node scripts/validate-site.mjs",
    "check-css": "npm run build-css && git diff --exit-code -- css/style.css",
    "test": "npm run validate-site && npm run check-css"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "sass": "1.104.1"
  }
}
